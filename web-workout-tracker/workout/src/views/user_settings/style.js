import styled from 'styled-components'

export default styled.section`
    .ideal-body-type {
        display: flex;
        .type {
            display: flex;
            flex-direction: column;
            align-items: center;

            span {
                font-size: 2rem;
            }
        }
    }
`